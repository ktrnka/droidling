These are all the Perl sources for training unigram models, cleaning up unigram models, and training/testing language identification models.

Some of the scripts are quite old and perhaps temporary; I need to clean this dir up.